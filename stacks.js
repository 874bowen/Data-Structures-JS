/* stacks! */

/*problem: find a palindrome word */

let letters = [];

let word = "racecar";

let rword = "";

// put the letters of the word into a stack
for (let i = 0; i <= word.length; i++){
    letters.push(word[i]);
}

